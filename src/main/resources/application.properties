spring.application.name=tcp-sync

# --- Real external TCP peer (run com.tr.tcpsync.mockserver.TcpHealthServer) ---
tcp.host=127.0.0.1
tcp.port=9099
tcp.client-id=tcp-sync-client
tcp.connect-timeout-ms=5000
# How long an HTTP request waits for the correlated TCP reply
tcp.request-timeout-ms=5000
